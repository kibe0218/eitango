全てのvmはrootに集める（ui状態保持の為とあと管理が楽）(Feature分割も面白そう)
vmがしていいのは状態の定義と変更
純粋ロジックはできるだけlogicフォルダに、ロジックの総まとめ的なロジックはusecaseに
viewはsessionに触れないようにするため、ほとんどの処理がここで完結することになる（viewはsessionをromるのはOK）
@Publishedと(observable)を持つことができる
